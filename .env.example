# Copy to .env.local and fill in. .env.local is git-ignored — never commit real values.
#
#   cp .env.example .env.local
#
# On Vercel these are set under Project Settings -> Environment Variables.
# Everything below is a placeholder: no real credential belongs in this file.

# --- Public (inlined into the browser bundle — put nothing secret here) ---

# Absolute base URL of this deployment, with no trailing slash.
#
# NO LONGER USED IN PRODUCTION, and deliberately so. The production canonical
# origin is CANONICAL_ORIGIN in lib/marketing/brand.ts, because which domain
# the public site is indexed under is a fact about the brand rather than a
# deployment setting. It previously lived only here, which is how production
# came to publish a canonical tag and an og:url pointing at an old preview
# deployment: this was set once, correctly, and then quietly went stale.
#
# What it still does: names the host for any NON-production deployment, ahead
# of Vercel's own deployment URL. Previews keep their own hostname on purpose,
# since Stripe's return URLs are built from the same value and a preview that
# claimed the canonical origin would drop a tester onto the live site mid
# checkout. In local development the fallback is http://localhost:3000.
#
# A localhost URL is never emitted in production: the app omits the canonical
# and refuses checkout instead.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Admin area (protects /admin/*) ---
#
# REQUIRED IN PRODUCTION. Without both, /admin returns 503 and stays locked —
# it never falls open. Use a long, randomly generated password; this is HTTP
# Basic auth over HTTPS, not a full identity system.
ADMIN_USERNAME=
ADMIN_PASSWORD=

# --- Stripe ---
#
# Checkout needs all three of the following. If any is missing, checkout runs
# in a preview mode locally and is disabled outright in production.
#
# Create two products in the Stripe dashboard, each with one price:
#   1. "Linwick: Setup",   GBP 149.00, one-off  -> STRIPE_PRICE_SETUP
#   2. "Linwick: Monthly", GBP 79.00, monthly   -> STRIPE_PRICE_MONTHLY
# These names are shown to the customer on the Stripe checkout page and on
# every invoice, so they have to match the brand on the site. If products
# already exist under an older name, rename them in the Stripe dashboard.
# Both ids start with "price_". Product ids ("prod_") will not work.
#
# Two products rather than one with two prices: Checkout renders the *product*
# name per line item, so a single product shows the customer the same name
# twice at two different amounts with nothing to tell them apart.
#
# Leave each price's tax behaviour "unspecified" until the VAT question in
# lib/pricing.ts is settled — it is immutable once set, and changing it means
# creating a new price and repointing these variables.

# Secret key. Use the test key (sk_test_...) until you are ready to take money.
STRIPE_SECRET_KEY=

# Price id for the one-off setup fee.
STRIPE_PRICE_SETUP=

# Price id for the recurring monthly subscription.
STRIPE_PRICE_MONTHLY=

# Signing secret for the webhook endpoint at /api/stripe/webhook. Without it
# that route returns 503 and accepts nothing — it never processes an unverified
# payload.
#
# Two different secrets, depending on where events come from:
#   - `stripe listen --latest --forward-to localhost:3000/api/stripe/webhook`
#     prints one for local development. It changes each time you start the CLI.
#   - A dashboard event destination has its own, shown once on creation.
#
# Payloads are serialised at the account's (or destination's) API version, not
# the SDK's, so aim for 2026-07-29.dahlia to match the pinned SDK. `--latest`
# does that locally. The account default can only be changed in live mode, so
# in test mode set `api_version` on the destination itself instead.
STRIPE_WEBHOOK_SECRET=

# --- Datastore (durable orders and leads) ---
#
# OPTIONAL. Without it, confirmed billing events are recorded to the server
# log only, exactly as before the datastore existed, and /admin/leads shows a
# "not configured" notice. With it, every billing event is also written to
# Postgres with de-duplication on Stripe's event id, so Stripe's retries can
# never create duplicate rows.
#
# Provisioning (ADR-1): one small managed Postgres-class database in an EU
# region. Which vendor is deliberately open (Neon, Supabase, Vercel Postgres,
# Turso) — anything speaking the Postgres wire protocol works. Paste the full
# connection string, including any ?sslmode parameter the vendor requires.
#
# Local development: scripts/dev-postgres.ps1 stands up a throwaway instance
# from portable binaries; no Docker or installer needed.
LEADS_DATABASE_URL=

# --- Retention sweep ---
#
# OPTIONAL. /api/retention deletes enquiry rows once their retention window
# has passed (30 days after delivery; 90 days hard ceiling). Set one of these
# and schedule the route: on Vercel, naming it CRON_SECRET makes the platform
# scheduler authenticate automatically (see vercel.json); RETENTION_SECRET
# works for any other scheduler. Unset, the route answers 503 - fail closed.
RETENTION_SECRET=
# CRON_SECRET=

# --- Lead email delivery (Resend) ---
#
# OPTIONAL, AND OFF BY DEFAULT - the same discipline as the AI model below:
# an API key alone never turns anything on. With EMAIL_SENDING_ENABLED unset
# or false, enquiries are stored and shown in the admin panel but NOTHING is
# sent, no request ever leaves for Resend, and /api/email-dispatch answers
# 503.
#
# Provisioning (ADR-2):
#   1. Create a Resend account and verify the sending domain linwick.co.uk
#      (SPF + DKIM records at the DNS host). The From address is hardcoded to
#      enquiries@linwick.co.uk in lib/email/resend.ts; replies go straight to
#      the customer via Reply-To, so the mailbox only needs to catch bounces.
#   2. RESEND_API_KEY: a sending key from the Resend dashboard.
#   3. Add a webhook endpoint in the dashboard pointing at
#      https://<your-domain>/api/webhooks/resend, subscribed to at least:
#      email.sent, email.delivered, email.delivery_delayed, email.bounced,
#      email.complained. RESEND_WEBHOOK_SECRET is the endpoint's signing
#      secret (whsec_...). Without it the webhook route answers 503 and
#      accepts nothing.
#
# What each state means (see lib/db/store.ts): captured = stored, nothing
# sent; pending = handed to Resend, delivery unconfirmed; sent = confirmed by
# webhook; undeliverable = failed permanently or retries exhausted, and an
# escalation with the full enquiry has gone to CONTACT_EMAIL.

EMAIL_SENDING_ENABLED=false
RESEND_API_KEY=
RESEND_WEBHOOK_SECRET=

# Internal owner alerts ("a tenant just went live"). OPTIONAL, and doubly
# gated: the alert fires only when EMAIL_SENDING_ENABLED is true (with an API
# key) AND this names a mailbox. Either one missing means no alert is ever
# attempted; the suppression is logged at the moment it happens. The alert is
# sent synchronously from the request that enables a tenant (verified Stripe
# payment + submitted setup form) — deliberately not from /api/email-dispatch,
# which on the Hobby tier runs once a day and could lag a sale by ~24h.
OWNER_NOTIFICATION_EMAIL=

# Retry schedule for transient send failures, milliseconds comma-separated.
# Defaults to 300000,900000,2400000 (5m/15m/40m - three attempts inside an
# hour). Override only to compress it for tests. Permanent failures never
# retry regardless of this value.
# EMAIL_RETRY_DELAYS_MS=300000,900000,2400000

# Bearer secret for /api/email-dispatch, the scheduled sweep that re-sends
# transient failures and escalates exhausted ones. Same convention as the
# retention secret above: CRON_SECRET makes Vercel's scheduler authenticate
# automatically. Unset, the route answers 503 - fail closed.
EMAIL_DISPATCH_SECRET=

# Test-only override of the Resend API origin (scripts/verify-phase3.mjs).
# Never set this outside a local verification run.
# RESEND_BASE_URL=https://api.resend.com

# --- AI model (DeepSeek) ---
#
# OPTIONAL, AND OFF BY DEFAULT. Without it the assistant runs on the
# deterministic offline engine, which works fully — it is just less naturally
# worded.
#
# The model never drives the conversation: it proposes what the customer said
# and suggests wording, and the offline engine remains the state machine. See
# app/api/assistant/route.ts.
#
# Requests are rate limited per IP (10/min, 60/hr) with a global daily ceiling.
# See lib/rate-limit.ts for what that does and does not guarantee.

# Master switch for the live model. Unset, blank or anything other than
# true/1/yes means OFF, and nothing is sent to DeepSeek regardless of the
# credentials below.
#
# DO NOT SET THIS TO true IN PRODUCTION. DeepSeek processes conversation
# content in China. Transferring personal data there needs a GDPR Chapter V
# basis (Art. 46 Standard Contractual Clauses plus a transfer impact
# assessment), and that is not in place — it is the open item in section 6 of
# BUSINESS_PLAN.md. Leave this off until it is.
ASSISTANT_MODEL_ENABLED=false

# Secret key from the DeepSeek console. Server-side only; never exposed.
# Ignored entirely while ASSISTANT_MODEL_ENABLED is off.
DEEPSEEK_API_KEY=

# Exact model id, copied from DeepSeek's own documentation. Deliberately has no
# default: a wrong or invented model id fails as a confusing 400 rather than an
# obvious misconfiguration.
DEEPSEEK_MODEL=

# Override only if DeepSeek changes its API origin.
# DEEPSEEK_BASE_URL=https://api.deepseek.com
